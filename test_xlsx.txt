python3 << 'PYEOF'
f = "/opt/conda/envs/DWS-GPU/lib/python3.11/site-packages/flashinfer/comm/fd_exchange.py"
lines = open(f, encoding="utf-8").read().split("\n")

# Où est __future__ actuellement ?
for i, ln in enumerate(lines):
    if "__future__" in ln:
        print(f"__future__ est à la ligne {i+1}: {ln!r}")

print("--- 20 premières lignes ---")
for i, ln in enumerate(lines[:20], 1):
    print(f"{i:3} | {ln}")

print("--- lignes contenant array.array[ ---")
for i, ln in enumerate(lines, 1):
    if "array.array[" in ln:
        print(f"{i:3} | {ln}")
PYEOF
