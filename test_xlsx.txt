MAP_COLONNES = {
    'ACTIF': {...},
    ...
}

# ── A COLLER ICI ─────────────────────────────────────────────
# Cellules calculees par le modele : on ne les ecrit jamais,
# la valeur extraite part en comparaison (totaux).
CALC_COLS = {
    'ACTIF': {'net_n', 'net_n1'},
    'A1':   {'solde_fin'},
    'A2':   {'solde_debiteur', 'solde_crediteur'},
    'A5':   {'dotations_cumulees_fin', 'ecarts'},
    'A7':   {'valeur_nette_comptable', 'plus_value', 'moins_value'},
    'A8':   {'provisions_cumulees_fin'},
    'A82':  {'valeur_nette_comptable'},
}
# ─────────────────────────────────────────────────────────────

MAP_LIGNES = { ... }





        ligne, calculee = ligne_info
        coord = colonne + str(ligne)
        cible = wb[MAP_FEUILLES[tab]][coord]
        if calculee or col in CALC_COLS.get(tab, set()) or _est_formule(cible):




                    cellule_cible = ws[colonne + str(r)]
                    if col in CALC_COLS.get(tab, set()) or _est_formule(cellule_cible):






